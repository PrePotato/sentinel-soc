# Screenshots

Save the app captures here using **these exact filenames** (referenced by the
root `README.md`):

| Filename | View to capture |
|----------|-----------------|
| `dashboard.png` | Full dashboard — summary bar, threat gauge, live network map, threat timeline, alert center |
| `login.png` | SENTINEL SOC login screen |
| `ports-and-packets.png` | Port Scanner + Packet Inspection (side by side) |
| `vulnerabilities-and-geo.png` | Vulnerability Scanner + Geo-IP Attack Origins (side by side) |
| `ai-and-reports.png` | AI Threat Analyst + Report Generation (side by side) |

**How to capture:** run the app (`docker compose up --build`, or the local dev
setup), open <http://localhost:8080> (or `:5173` in dev), log in with
`admin` / `admin`, and screenshot each view.
